import { NavItem } from './types';

export const NAV_ITEMS: NavItem[] = [
  { label: 'Dashboard', path: '/' },
  { label: 'Add Task', path: '/create' },
  { label: 'Pipeline', path: '/list' },
];

export const DB_KEY = 'DEADLINE_BOT_DB';